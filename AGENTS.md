<!-- AI_SPEC_BEGIN -->
# 工程规范
> 由 AI Coder 自动生成，如需调整请在项目设置中修改技能配置。

---

# Git 提交规范

## 核心原则

- 提交信息格式：`<type>(<scope>): <subject>`
- type 取值：`feat` / `fix` / `refactor` / `docs` / `test` / `chore` / `perf`
- 每次提交只做一件事，保持原子性
- subject 用中文或英文均可，但要清晰描述"做了什么"
- 提交前确认 diff 符合预期，不提交多余文件

## 禁止事项

- 禁止 commit message 只写 "fix"、"update"、"wip" 等无意义内容
- 禁止一次提交包含多个不相关的改动
- 禁止提交调试代码、临时注释、console.log 等残留
- 禁止提交 .env、密钥文件、大型二进制文件

## 示例

```
feat(auth): 添加 JWT 双 token 刷新机制
fix(api): 修复分页参数越界导致的 500 错误
refactor(db): 将 pymysql 连接改为连接池模式
docs: 补充 API 接口文档
```


---

# 安全编码底线

## 核心原则

- 禁止在代码中硬编码密码、token、密钥、私钥
- 所有外部输入必须验证，不可直接拼接到 SQL / 命令行 / HTML
- 文件操作必须做路径安全检查，防止路径穿越（`../` 攻击）
- 敏感配置统一通过环境变量或配置文件读取

## 禁止事项

- 禁止 SQL 拼接字符串，必须使用参数化查询
- 禁止将用户输入直接传给 `os.system()` / `subprocess` / `eval()`
- 禁止在日志中记录密码、token、完整信用卡号等敏感字段
- 禁止忽略 SSL 证书验证（`verify=False`）用于生产环境
- 禁止在前端存储敏感数据（localStorage 中存 token 需评估风险）

## 验证标准

- 代码中无硬编码凭证（grep 检查 `password=`, `secret=`, `api_key=` 等字面量）
- 数据库查询全部使用占位符（`%s` / `?`）
- 文件路径操作有 `os.path.realpath` 或白名单校验


---

# 代码风格规范

## 核心原则

- 命名要表达意图，避免缩写（`user_count` 优于 `uc`）
- 函数只做一件事，超过 40 行考虑拆分
- 注释说明"为什么"，而不是"做了什么"（代码本身说明做什么）
- 保持一致的缩进和格式（跟随项目现有风格）

## 禁止事项

- 禁止魔法数字，用命名常量替代（`MAX_RETRY = 3` 而非直接写 `3`）
- 禁止深度嵌套（超过 3 层考虑提前 return 或抽函数）
- 禁止重复代码（DRY 原则，相同逻辑超过 2 处即抽取）
- 禁止无意义的注释（`# 加一` 之类）

## 验证标准

- 新增函数有清晰的函数名，参数名有意义
- 无超过 3 层的 if/for 嵌套
- 无复制粘贴的重复代码块


---

# 错误处理规范

## 核心原则

- 错误要被处理或向上传播，不可静默吞掉（空 except 块）
- 错误信息要包含足够上下文（操作是什么、参数是什么、错误原因）
- 区分"预期错误"（业务逻辑）和"异常错误"（程序 bug）
- 对外接口统一返回结构化错误（含错误码和可读消息）

## 禁止事项

- 禁止 `except: pass` 或 `except Exception: pass`（静默吞掉所有错误）
- 禁止在 except 块中只打印错误而不处理（除非明确记录在注释）
- 禁止用错误码代替清晰的错误消息
- 禁止在用户可见的错误中暴露堆栈跟踪或内部路径

## 验证标准

- 每个 try/except 都有明确的处理逻辑或 re-raise
- 日志记录包含足够诊断信息
- HTTP 接口错误响应有统一格式

<!-- AI_SPEC_END -->
